function h = ham(graph)
n = length(graph);
h = graph;
for i = 1:n
   row = i;
   link = find(graph(i,:)== 1);
   for k = 1:length(link)
       for j = 1:5
           if (graph(link(k),i) = 1 && i ~= row)
               h(link(k))
   end
end